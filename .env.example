# Contact form relay (src/app/api/contact/route.ts). Get a free key at resend.com.
RESEND_API_KEY=

# Race Mode leaderboard (src/app/api/leaderboard/route.ts). Optional — the route
# degrades to an empty/offline leaderboard if unset. Get free credentials at upstash.com.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

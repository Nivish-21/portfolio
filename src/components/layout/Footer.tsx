export function Footer() {
  return (
    <footer className="mx-auto w-full max-w-content px-6 md:px-8 py-6 border-t border-line-strong">
      <div className="flex flex-wrap items-center justify-between gap-2 font-mono text-[10px] uppercase tracking-[0.1em] text-muted">
        <span>Nivish Vincent Raj &middot; Tamil Nadu, India</span>
        <span>Ship first. Scale later.</span>
      </div>
    </footer>
  );
}
